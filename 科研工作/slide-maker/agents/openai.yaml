interface:
  display_name: "slide maker"
  short_description: "把资料、大纲或自然语言需求生成学术 PPTX，支持 NotebookLM-style 全图像化 deck"
  default_prompt: "使用 $slide maker 把资料、大纲或自然语言需求生成默认 Image2 全图像化 PPTX；只有明确要求可编辑时才走原生可编辑模式："
policy:
  allow_implicit_invocation: true
